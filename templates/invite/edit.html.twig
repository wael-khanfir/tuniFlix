{% extends 'base.html.twig' %}

{% block title %}Edit Invite{% endblock %}

{% block body %}
    <h1>Edit Invite</h1>

    {{ include('invite/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('invite_index') }}">back to list</a>

    {{ include('invite/_delete_form.html.twig') }}
{% endblock %}
